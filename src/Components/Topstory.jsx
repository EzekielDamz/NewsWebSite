// import { NewApi } from "../contants"

const Topstory = () => {
  return (
      <main>
          
      {/* <div className="grid grid-cols-2 gap-5">
        {NewApi.map((items, index) => (
          <div key={index}>
            <div className="">
              <img src={items.imgUrl} alt="" />
            </div>
          </div>
        ))}
      </div> */}
    </main>
  );
}

export default Topstory
